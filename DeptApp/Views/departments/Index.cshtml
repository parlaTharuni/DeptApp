@model IEnumerable<DeptApp.department>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Deptname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DeptLoc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Deptname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeptLoc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Deptno }) |
            @Html.ActionLink("Details", "Details", new { id=item.Deptno }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Deptno })
        </td>
    </tr>
}

</table>
